
> g++ mainS.cpp linked_list.cpp

> ./a.out

> valgrind ./a.out

Along with the above, it will ask for inputs. It will ask the amount of nodes you would like to add, values for them (will only permit 1, -1), then proceed to show some of the functions available to the program. It will ask to delete a node and calculate the prefix sum, as well as constantly reminded the new size of the list. The user is free to add more computations to the list, and further testing is done on linked_list_test.cpp.

